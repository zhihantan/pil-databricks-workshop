# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
env/
.python-version

# Node / frontend
app/frontend/node_modules/
app/frontend/dist/
# NOTE: app/backend/static/ (the built Vite bundle) IS committed on purpose:
# Databricks Apps deploy from the Git-folder source with no build step, so the
# SPA must ship prebuilt. Rebuild with `cd app/frontend && npm run build`.
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Databricks
.databricks/
.databricks-login.json

# Local generated artifacts (data is generated in-workspace, never committed)
/tmp_local_data/
*.local.env
.env

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
MASTER_PROMPT_PIL_WORKSHOP.md

# Local MLflow tracking output (from running notebook 11 off-platform)
setup/mlruns/
setup/mlflow.db
# TS/Vite compiled outputs
app/frontend/vite.config.js
app/frontend/vite.config.d.ts
*.tsbuildinfo
