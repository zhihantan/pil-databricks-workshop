# PIL App backend dependencies.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
databricks-sdk>=0.40.0
databricks-sql-connector>=3.6.0
# Postgres driver for Lakebase (psycopg v3).
psycopg[binary]>=3.1
openai>=1.40          # OpenAI-compatible client for governed FMAPI (never api.openai.com)

# Test-only
pytest>=8.2
httpx>=0.27           # required by fastapi TestClient
